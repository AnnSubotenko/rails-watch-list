<h3>Add a movie bookmark to your list!</h3>

<%= simple_form_for [ @list, @movie, @bookmark ] do |f| %>
  <%= f.input :comment %>
  <%= f.association :movie %>
  <%= f.association :list %>
  <%= f.button :submit %>
<% end %>
